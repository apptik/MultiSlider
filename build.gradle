// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    group = GROUP
    version = VERSION_NAME
    repositories {
        google()
        mavenCentral()
    }
}

ext {
    minSdkVersion = 14
    compileSdkVersion = 28
    buildToolsVersion = '28.0.3'

    supportAnnotations = "androidx.annotation:annotation:1.0.0"
    supportAppCompat = "androidx.appcompat:appcompat:1.0.0"
    supportCardView = "androidx.cardview:cardview:1.0.0"
    supportCompat = "androidx.core:core:1.0.0"
    supportDesign = "com.google.android.material:material:1.0.0-rc01"
    supportRecyclerView = "androidx.recyclerview:recyclerview:1.0.0"

    supportTestRunner = "androidx.test:runner:1.1.0"
    supportTestRules = "androidx.test:rules:1.1.0"
    supportTestEspresso = "androidx.test.espresso:espresso-core:3.1.0"
    supportTestEspressoContrib = "androidx.test.espresso:espresso-contrib:3.1.0"
    supportTestUiautomator = "androidx.test.uiautomator:uiautomator:2.2.0"

    junit = "androidx.test.ext:junit:1.0.0"

    assertjCore1 = 'org.assertj:assertj-core:1.7.1'
    assertjCore2 = 'org.assertj:assertj-core:2.5.0'
    assertjCore3 = 'org.assertj:assertj-core:3.5.2'
    mockitoCore = 'org.mockito:mockito-core:2.0.99-beta'
}
